import { withClasses } from "../../utils/with-classes";
import classes from "./suggest-connected.module.scss";
import { SuggestConnected as SC } from "./suggest-connected";

export const SuggestConnected = withClasses(classes, SC);

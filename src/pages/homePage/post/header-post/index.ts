import { withClasses } from "../../../../utils/with-classes";
import classes from "./header-post.module.scss";
import { HeaderPost as HP } from "./header-post";

export const HeaderPost = withClasses(classes, HP);

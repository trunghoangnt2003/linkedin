import { withClasses } from "../../../../utils/with-classes";
import classes from "./image-modal.module.scss";
import { ImageGalleryModal as IM } from "./image-modal";

export const ViewImageModal = withClasses(classes, IM);

import { withClasses } from "../../utils/with-classes";
import classes from "./notification.module.scss";
import { NotificationList as NC } from "./notification";

export const NotificationList = withClasses(classes, NC);

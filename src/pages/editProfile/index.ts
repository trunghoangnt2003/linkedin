import { withClasses } from "../../utils/with-classes";
import classes from "./edit-profile.module.scss";
import { EditProfile as EP } from "./edit-profile";

export const EditProfile = withClasses(classes, EP);

import classes from "./description.module.scss";
import { Description as D } from "./description";
import { withClasses } from "../../../utils/with-classes";

export const Description = withClasses(classes, D);
